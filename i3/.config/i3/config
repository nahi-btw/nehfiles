# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:Caladea 12
font pango:Terminess Nerd Font Mono 20
# font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3
# exec --no-startup-id polybar &
exec_always --no-startup-id sxhkd &
exec_always --no-startup-id ~/scripts/keys.sh &
# exec_always --no-startup-id ~/scripts/i3-swallow.sh &
# exec_always --no-startup-id ~/scripts/wall.sh &
exec_always ~/.fehbg
exec --no-startup-id ~/.screenlayout/ACTUAL_LAYOUT.sh &
# exec_always --no-startup-id redshift -P -O 4500
# exec_always --no-startup-id /home/nahi-btw/scripts/omnisearch.sh
#exec_always --no-startup-id ~/.config/polybar/launch.sh
# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
# Emacs
# exec --no-startup-id emacs --daemon

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec_always --no-startup-id nm-applet
exec --no-startup-id nm-applet
exec_always --no-startup-id blueman-applet
exec_always --no-startup-id flameshot
exec_always --no-startup-id picom -b
exec_always --no-startup-id killall pasystray && pasystray
#exec_always --no-startup-id nitrgen --restore
# Wallpaper
# exec_always --no-startup-id hsetroot -solid "#002b36" -center ~/Pictures/sol-forest.png
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +05% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -05% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Brightness control
bindsym $mod+Prior exec --no-startup-id brightnessctl set 10%+
bindsym $mod+Next exec --no-startup-id brightnessctl set 10%-
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec kitty
# The Helix Editor
bindsym $mod+g exec kitty -e helix 

# VIM (VI IMproved)
# bindsym $mod+v exec gvim
# kill focused window
bindsym $mod+q kill

# Switch Windows
bindsym $mod+Tab exec focus next
bindsym $mod+Shift+Tab exec focus prev

# start dmenu (a program launcher)
bindsym $mod+d exec rofi -show drun --icons
# bindsym $mod+b exec ~/scripts/bar-toggle.sh
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"

# Scratchpads
bindsym $mod+Shift+BackSpace move scratchpad
bindsym $mod+backslash scratchpad show
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop
# Notification
exec --no-statup-id dunst &
# change focus
bindsym $mod+h focus left 
bindsym $mod+j focus down
bindsym $mod+k focus up 
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+o split h

# split in vertical orientation
bindsym $mod+p split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for workspaces. One per monitor, plus a "trash"/background one.
set $ws1 "1:Main"
set $ws2 "2:Sec"
set $ws3 "3:Trash"

# switch to workspace (non-numeric binds: m, n, u)
bindsym $mod+m workspace number $ws1
bindsym $mod+n workspace number $ws2
bindsym $mod+u workspace number $ws3

# move focused container to workspace
bindsym $mod+Shift+m move container to workspace number $ws1
bindsym $mod+Shift+n move container to workspace number $ws2
bindsym $mod+Shift+u move container to workspace number $ws3

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 7 px or 7 ppt
        bindsym j resize grow height 7 px or 7 ppt
        bindsym k resize shrink height 7 px or 7 ppt
        bindsym l resize grow width 7 px or 7 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bar {
     status_command i3status -c ~/.config/i3status/config
     position bottom
     tray_output primary
     font pango:Terminess Nerd Font Bold 15

     colors {
         background #2D353B           # Everforest bg
         statusline #D3C6AA           # Everforest fg
         separator  #3D484D           # Everforest bg-alt-ish

         # Workspace buttons
         #                  border       background     text
         focused_workspace  #A7C080  #A7C080  #2D353B    # Green
         active_workspace   #83C092  #83C092  #2D353B    # Teal-green
         inactive_workspace #3D484D  #3D484D  #859289    # Muted
         urgent_workspace   #E67E80  #E67E80  #2D353B    # Red
         binding_mode       #DBBC7F  #DBBC7F  #2D353B    # Yellow
     }
}
#  Gaps
 gaps inner 5
 gaps outer 4
# smart_gaps on
## Solarized borders
default_border pixel 1
default_floating_border pixel 2

# All purple borders
#default_border pixel 3
default_floating_border pixel 3

# client.focused          #cb4b16 #cb4b16 #002b36 #cb4b16
# client.focused_inactive #657b83 #657b83 #002b36 #657b83
# client.unfocused        #073642 #073642 #839496 #073642
# client.urgent           #dc322f #dc322f #002b36 #dc322f
# client.placeholder      #2aa198 #2aa198 #002b36 #2aa198

client.focused          #a7c080 #a7c080 #2d353b #a7c080
client.focused_inactive #859289 #859289 #2d353b #859289
client.unfocused        #3d484d #3d484d #9da9a0 #3d484d
client.urgent           #e67e80 #e67e80 #2d353b #e67e80
client.placeholder      #83c092 #83c092 #2d353b #83c092

# Optional: Add slight transparency
#for_window [class=".*"] border pixel 3

# Apps opening on a specific workspace
# assign [class="org.gnome.Rhythmbox3"] $ws5
# Window Rules
# for_window [class="copyq"] floating enable
# for_window [class="Places"] floating enable
# for_window [class="pavucontrol"] floating enable, move position 1600px 900px
# for_window [class="Blueman-manager"] floating enable
# for_window [class="PortProton"] floating enable
# for_window [class="firefox"] move to workspace $ws2
# for_window [class="zen"] move to workspace $ws2
# for_window [class="Brave-browser"] move to workspace $ws2
# for_window [class="Rhythmbox3"] move to workspace $ws5
# for_window [class="com.github.neithern.g4music"] move to workspace $ws5
# for_window [class="mpv"] move to workspace $ws5, border_pixel none
# for_window [class="Zathura"] move to workspace $ws4
# for_window [class="neovide"] move to workspace $ws3
# for_window [class="neovide"] move to workspace $ws3
# for_window [class="VSCodium"] move to workspace $ws3 
# for_window [class="Gvim"] move to workspace $ws3 
# for_window [class="Min"] move to workspace $ws2
# for_window [class="qutebrowser"] move to workspace $ws2
# for_window [class="librewolf"] move to workspace $ws2
# for_window [class="Pcmanfm"] move to workspace $ws4
# for_window [class="Alacritty"] move to workspace $ws1
